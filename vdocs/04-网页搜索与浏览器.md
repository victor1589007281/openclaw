# 网页搜索与浏览器自动化

## 1. 概述

OpenClaw 提供三种网页资料获取能力：

| **工具** | **用途** | **实现文件** |
|:---:|:---|:---|
| **web_search** | 关键词搜索，返回搜索结果列表 | `src/agents/tools/web-search.ts` |
| **web_fetch** | 抓取指定 URL 的可读内容 | `src/agents/tools/web-fetch.ts` |
| **browser** | 完整浏览器自动化控制 | `src/agents/tools/browser-tool.ts` |

三者协同工作：**搜索 → 发现 URL → 抓取/浏览内容**，形成完整的网页信息获取链路。

## 2. Web Search 搜索引擎实现

### 2.1 搜索提供商

OpenClaw 支持两种搜索引擎后端：

```mermaid
graph TB
    subgraph "**web_search 工具**"
        INPUT[**用户查询<br/>query + count + country + lang**]
        CACHE[**缓存检查<br/>15分钟 TTL / 最多100条**]
        PROVIDER[**Provider 路由**]
    end

    subgraph "**Brave Search**"
        BRAVE_API[**Brave Search API<br/>api.search.brave.com**]
        BRAVE_KEY[**BRAVE_API_KEY<br/>环境变量或配置**]
        BRAVE_RESULT[**结构化结果<br/>title / url / description / date**]
    end

    subgraph "**Perplexity Sonar**"
        PPLX_API[**Perplexity API<br/>api.perplexity.ai**]
        PPLX_OR[**OpenRouter API<br/>openrouter.ai/api/v1**]
        PPLX_RESULT[**AI 合成回答<br/>content + citations**]
    end

    subgraph "**结果处理**"
        WRAP[**安全包装<br/>wrapWebContent**]
        RETURN[**JSON 结果返回**]
    end

    INPUT --> CACHE
    CACHE -->|**命中**| RETURN
    CACHE -->|**未命中**| PROVIDER

    PROVIDER -->|**provider=brave**| BRAVE_API
    PROVIDER -->|**provider=perplexity**| PPLX_API
    PROVIDER -->|**provider=perplexity<br/>OpenRouter Key**| PPLX_OR

    BRAVE_API --> BRAVE_KEY
    BRAVE_KEY --> BRAVE_RESULT
    BRAVE_RESULT --> WRAP

    PPLX_API --> PPLX_RESULT
    PPLX_OR --> PPLX_RESULT
    PPLX_RESULT --> WRAP

    WRAP --> RETURN

    style INPUT fill:#e1f5ff,stroke:#333,stroke-width:2px,color:#000
    style CACHE fill:#e1f5ff,stroke:#333,stroke-width:2px,color:#000
    style PROVIDER fill:#e1f5ff,stroke:#333,stroke-width:2px,color:#000

    style BRAVE_API fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000
    style BRAVE_KEY fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000
    style BRAVE_RESULT fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000

    style PPLX_API fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style PPLX_OR fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style PPLX_RESULT fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000

    style WRAP fill:#f5e1ff,stroke:#333,stroke-width:2px,color:#000
    style RETURN fill:#f5e1ff,stroke:#333,stroke-width:2px,color:#000
```

### 2.2 Brave Search 详细参数

| **参数** | **类型** | **默认值** | **说明** |
|:---|:---:|:---:|:---|
| `query` | string | 必填 | 搜索关键词 |
| `count` | number | 5 | 返回结果数量（1-10） |
| `country` | string | US | 地区代码（2位） |
| `search_lang` | string | - | 搜索语言 ISO 代码 |
| `ui_lang` | string | - | 界面语言 ISO 代码 |
| `freshness` | string | - | 时效过滤：pd/pw/pm/py 或日期范围 |

### 2.3 Perplexity Sonar 详细参数

| **属性** | **详情** |
|:---|:---|
| **默认模型** | `perplexity/sonar-pro` |
| **接口格式** | Chat Completions API（OpenAI 兼容） |
| **直连端点** | `https://api.perplexity.ai` |
| **OpenRouter** | `https://openrouter.ai/api/v1` |
| **API Key** | `PERPLEXITY_API_KEY` 或 `OPENROUTER_API_KEY` |
| **返回格式** | AI 合成内容 + citations 引用数组 |

## 3. 搜索 API 定价与国内可用性

### 3.1 API 定价对比

| **提供商** | **免费额度** | **付费价格** | **认证方式** |
|:---:|:---|:---|:---:|
| **Brave Search** | **有** - 每月 2,000 次请求 | Base: $3/千次, Pro: $5/千次 | API Key + 信用卡注册 |
| **Perplexity Sonar** | **无** | Sonar: $1/百万 token, Sonar Pro: $3 入/$15 出/百万 token | API Key |
| **OpenRouter 转发** | **无** | 同 Perplexity 定价 + OpenRouter 加价 | API Key（支持加密货币） |

### 3.2 Brave Search 定价详情

| **套餐** | **价格** | **限制** | **功能** |
|:---|:---:|:---|:---|
| **Free** | **$0/月** | 1 QPS, 2,000 次/月 | Web搜索 + 新闻 + 视频 + 图片 + AI 数据权 |
| **Base** | **$3/千次** | 提高 QPS | 同 Free + 更高并发 |
| **Pro** | **$5/千次** | 最高 QPS | 全部功能 + 优先级 |

> **注意**：Free 套餐需通过 Stripe 绑定信用卡，但不收费。

### 3.3 Perplexity Sonar 定价详情

| **模型** | **输入价格** | **输出价格** | **上下文窗口** | **特点** |
|:---|:---:|:---:|:---:|:---|
| **Sonar** | $1/百万 token | $1/百万 token | 127K | 快速问答 + 网页搜索 |
| **Sonar Pro** | $3/百万 token | $15/百万 token | 200K | 多步推理 + 网页搜索（默认） |
| **Sonar Reasoning Pro** | $2/百万 token | $8/百万 token | 128K | 思维链分析 |
| **Search API** | $5/千次（固定） | - | - | 无 token 计费 |

### 3.4 国内可用性分析

```mermaid
graph TB
    subgraph "**国内可用性评估**"
        BRAVE_CN[**Brave Search API<br/>api.search.brave.com**]
        PPLX_CN[**Perplexity API<br/>api.perplexity.ai**]
        OR_CN[**OpenRouter API<br/>openrouter.ai**]
    end

    subgraph "**访问状态**"
        BRAVE_ST[**Brave: 需代理<br/>api.search.brave.com<br/>国内无法直连**]
        PPLX_ST[**Perplexity: 需代理<br/>api.perplexity.ai<br/>国内无法直连**]
        OR_ST[**OpenRouter: 需代理<br/>openrouter.ai<br/>国内无法直连**]
    end

    subgraph "**解决方案**"
        PROXY[**HTTP 代理<br/>配置 HTTP_PROXY**]
        VPN[**VPN/隧道<br/>全局代理**]
        DEPLOY[**海外部署<br/>Fly.io / Docker**]
    end

    BRAVE_CN --> BRAVE_ST
    PPLX_CN --> PPLX_ST
    OR_CN --> OR_ST

    BRAVE_ST --> PROXY
    PPLX_ST --> PROXY
    OR_ST --> PROXY

    BRAVE_ST --> VPN
    PPLX_ST --> VPN
    OR_ST --> VPN

    BRAVE_ST --> DEPLOY
    PPLX_ST --> DEPLOY
    OR_ST --> DEPLOY

    style BRAVE_CN fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000
    style PPLX_CN fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000
    style OR_CN fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000

    style BRAVE_ST fill:#fff3e1,stroke:#333,stroke-width:2px,color:#000
    style PPLX_ST fill:#fff3e1,stroke:#333,stroke-width:2px,color:#000
    style OR_ST fill:#fff3e1,stroke:#333,stroke-width:2px,color:#000

    style PROXY fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style VPN fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style DEPLOY fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
```

| **API** | **国内直连** | **原因** | **解决方案** |
|:---|:---:|:---|:---|
| **Brave Search** | **不可** | `api.search.brave.com` 被墙 | 代理 / VPN / 海外服务器部署 |
| **Perplexity** | **不可** | `api.perplexity.ai` 被墙 | 代理 / VPN / 海外服务器部署 |
| **OpenRouter** | **不可** | `openrouter.ai` 被墙 | 代理 / VPN / 海外服务器部署 |

> **最佳实践**：国内用户建议将 OpenClaw Gateway 部署在海外服务器（如 Fly.io），通过 WebSocket 连接使用，搜索 API 调用在海外服务器上完成，无需本地代理。

### 3.5 免费替代方案与代理方案

OpenClaw 内置支持 Brave Search 和 Perplexity，但国内用户也可以考虑以下免费替代方案：

| **服务** | **免费额度** | **是否需注册** | **国内可用性** | **特点** |
|:---|:---|:---:|:---:|:---|
| **Brave Search** | 2,000 次/月 | 需要（绑信用卡） | 需代理 | 独立索引，结构化结果 |
| **Search1API** | 100 次免费额度 | 可选（无需信用卡） | 需代理 | 支持 Google/Bing/DuckDuckGo 等多引擎 |
| **Tavily** | 有免费套餐 | 需要 | 需代理 | AI 原生搜索，聚合 20+ 来源 |
| **Firecrawl Search** | 有免费套餐 | 需要 | 需代理 | 支持 Markdown/HTML/截图输出 |
| **SearXNG 自建** | **完全免费** | **不需要** | **可自建** | 开源元搜索引擎，聚合多搜索引擎 |

**国内推荐方案 — SearXNG 自建**：

SearXNG 是一个开源的元搜索引擎，可以自建在国内服务器上，无需 API Key，聚合 Google、Bing、DuckDuckGo 等搜索引擎的结果。虽然 OpenClaw 目前不原生支持 SearXNG，但可以通过自定义 Skill 或 Plugin 集成。

```text
# 国内用户推荐方案：

方案 1 - 海外部署 Gateway（推荐）:
  ┌─────────────────────────────────────────┐
  │ 国内设备 → WebSocket → 海外 Fly.io Gateway │
  │                          ↓               │
  │              Brave Search API / Perplexity │
  └─────────────────────────────────────────┘
  优点：零配置代理，Brave 免费 2000 次/月
  缺点：需要海外服务器

方案 2 - SearXNG 自建:
  ┌─────────────────────────────────────────┐
  │ OpenClaw → 自定义 Plugin → 自建 SearXNG   │
  │                             ↓            │
  │                   Google / Bing 聚合结果   │
  └─────────────────────────────────────────┘
  优点：完全免费，无 API Key，国内可用
  缺点：需自建维护，需编写 Plugin

方案 3 - HTTP 代理:
  ┌─────────────────────────────────────────┐
  │ OpenClaw → HTTP_PROXY → Brave/Perplexity │
  └─────────────────────────────────────────┘
  优点：不改架构，直接配环境变量
  缺点：需要稳定代理
```

### 3.6 API Key 路由逻辑

系统根据 API Key 前缀自动判断使用哪个端点：

| **Key 前缀** | **路由目标** | **端点** |
|:---|:---|:---|
| `pplx-` | Perplexity 直连 | `https://api.perplexity.ai` |
| `sk-or-` | OpenRouter 转发 | `https://openrouter.ai/api/v1` |
| 其他格式 | 默认 OpenRouter | `https://openrouter.ai/api/v1` |

### 3.6 错误处理与容错

| **场景** | **处理方式** |
|:---|:---|
| **API Key 缺失** | 返回 `missing_brave_api_key` / `missing_perplexity_api_key` 错误 + 配置说明 |
| **API 调用失败** | 抛出异常，返回 HTTP 状态码和错误详情 |
| **Provider 间无自动切换** | Brave 失败不会自动切换到 Perplexity，反之亦然 |
| **超时** | AbortSignal 30 秒超时 |
| **缓存命中** | 直接返回缓存，15 分钟 TTL |

## 4. Web Search 完整时序图（补充：含缓存与 API Key 路由）

```mermaid
sequenceDiagram
    participant AGENT as **Agent Runtime**
    participant SEARCH as **web_search 工具**
    participant LOCALCACHE as **内存缓存**
    participant BRAVE as **Brave Search API**
    participant PPLX as **Perplexity API**

    AGENT->>SEARCH: **1. tool_use: web_search<br/>query="OpenClaw architecture"**
    SEARCH->>SEARCH: **2. 参数验证<br/>TypeBox Schema**
    SEARCH->>SEARCH: **3. 解析 Provider<br/>从配置读取**

    SEARCH->>LOCALCACHE: **4. 检查缓存<br/>key = provider+query+count+country+lang+freshness**

    alt **缓存命中且未过期 - TTL 15min**
        LOCALCACHE-->>SEARCH: **5a. 返回缓存结果**
    else **缓存未命中 - 调用 Brave Search**
        SEARCH->>BRAVE: **5b. GET /res/v1/web/search<br/>Headers: X-Subscription-Token**
        BRAVE-->>SEARCH: **6. 返回搜索结果<br/>web.results 数组**
        SEARCH->>SEARCH: **7. 映射结果字段<br/>title / url / description / date / siteName**
    else **缓存未命中 - 调用 Perplexity**
        SEARCH->>PPLX: **5c. POST /chat/completions<br/>model: sonar-pro**
        PPLX-->>SEARCH: **6. 返回 AI 合成回答<br/>content + citations**
    end

    SEARCH->>LOCALCACHE: **8. 写入缓存**
    SEARCH->>SEARCH: **9. wrapWebContent<br/>安全标记外部内容**
    SEARCH-->>AGENT: **10. 返回 JSON 结果<br/>query / provider / count / tookMs / results**

    rect rgb(255, 250, 205)
    Note over AGENT,SEARCH: **Agent 可根据搜索结果调用 web_fetch 获取详细内容**
    end
```

## 4. Web Fetch 网页抓取

### 4.1 抓取流程

`web_fetch` 工具用于从指定 URL 获取可读内容：

```mermaid
graph TB
    subgraph "**web_fetch 输入**"
        URL_INPUT[**URL 地址**]
        FORMAT[**输出格式<br/>markdown / text**]
    end

    subgraph "**安全检查**"
        SSRF[**SSRF 防护<br/>fetch-guard.ts**]
        TIMEOUT[**超时控制<br/>30秒默认**]
    end

    subgraph "**内容提取**"
        FETCH_HTML[**HTTP 请求<br/>获取 HTML**]
        READABILITY[**Readability.js<br/>提取正文**]
        FIRECRAWL[**Firecrawl<br/>复杂页面抓取（可选）**]
    end

    subgraph "**内容处理**"
        TO_MD[**HTML 转 Markdown<br/>web-fetch-utils.ts**]
        TRUNCATE[**内容截断<br/>默认 50000 字符**]
        SECURE[**安全包装<br/>标记外部内容**]
    end

    URL_INPUT --> SSRF
    FORMAT --> SSRF
    SSRF --> TIMEOUT
    TIMEOUT --> FETCH_HTML
    FETCH_HTML --> READABILITY
    FETCH_HTML -.->|**复杂页面**| FIRECRAWL
    READABILITY --> TO_MD
    FIRECRAWL --> TO_MD
    TO_MD --> TRUNCATE
    TRUNCATE --> SECURE

    style URL_INPUT fill:#e1f5ff,stroke:#333,stroke-width:2px,color:#000
    style FORMAT fill:#e1f5ff,stroke:#333,stroke-width:2px,color:#000
    style SSRF fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000
    style TIMEOUT fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000
    style FETCH_HTML fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style READABILITY fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style FIRECRAWL fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style TO_MD fill:#f5e1ff,stroke:#333,stroke-width:2px,color:#000
    style TRUNCATE fill:#f5e1ff,stroke:#333,stroke-width:2px,color:#000
    style SECURE fill:#f5e1ff,stroke:#333,stroke-width:2px,color:#000
```

### 4.2 安全机制

| **防护措施** | **说明** |
|:---|:---|
| **SSRF 防护** | `fetch-guard.ts` 阻止访问内网地址（localhost、私有 IP） |
| **超时保护** | 可配置超时时间，默认 30 秒 |
| **内容截断** | 默认限制 50,000 字符，防止内存溢出 |
| **安全标记** | 外部内容用安全标记包装，防止 Prompt 注入 |
| **缓存限制** | 最多 100 条缓存，防止内存泄漏 |

## 6. Browser 浏览器自动化

### 6.1 支持的浏览器 — 仅 Chromium 内核

OpenClaw 的浏览器自动化**仅支持 Chromium 内核浏览器**，不支持 Firefox 和 Safari/WebKit。

| **浏览器** | **支持** | **检测方式** |
|:---|:---:|:---|
| **Google Chrome** | **是**（Stable / Beta / Canary / Dev） | Bundle ID / 注册表 |
| **Microsoft Edge** | **是**（Stable / Beta / Dev / Canary） | Bundle ID / 注册表 |
| **Brave** | **是**（Stable / Beta / Nightly） | Bundle ID / 注册表 |
| **Chromium** | **是** | 路径检测 |
| **Vivaldi** | **是** | 检测为 Chromium |
| **Opera / Opera GX** | **是** | 检测为 Chromium |
| **Arc** | **是** | 检测为 Chromium |
| **Yandex** | **是** | 检测为 Chromium |
| **Firefox** | **否** | - |
| **Safari** | **否** | - |

> **底层原因**：Playwright 仅作为 CDP（Chrome DevTools Protocol）客户端使用，通过 `chromium.connectOverCDP()` 连接，而非通过 Playwright 启动浏览器。因此只支持 CDP 协议的 Chromium 内核浏览器。

### 6.2 两种运行模式对比

```mermaid
graph LR
    subgraph "**openclaw 模式 - 隔离实例**"
        OC_LAUNCH[**OpenClaw 启动<br/>Chrome/Brave/Edge**]
        OC_DATA[**独立数据目录<br/>~/.openclaw/browser/<br/>openclaw/user-data**]
        OC_CDP[**--remote-debugging-port<br/>CDP 端口 18800-18899**]
        OC_PW[**Playwright<br/>connectOverCDP**]
    end

    subgraph "**chrome 模式 - 扩展中继**"
        CH_EXT[**Chrome 扩展<br/>安装到用户浏览器**]
        CH_RELAY[**Relay Server<br/>localhost:18792**]
        CH_WS[**WebSocket 连接<br/>/extension 端点**]
        CH_PW[**Playwright<br/>connectOverCDP**]
    end

    OC_LAUNCH --> OC_DATA
    OC_DATA --> OC_CDP
    OC_CDP --> OC_PW

    CH_EXT --> CH_WS
    CH_WS --> CH_RELAY
    CH_RELAY --> CH_PW

    style OC_LAUNCH fill:#e1f5ff,stroke:#333,stroke-width:2px,color:#000
    style OC_DATA fill:#e1f5ff,stroke:#333,stroke-width:2px,color:#000
    style OC_CDP fill:#e1f5ff,stroke:#333,stroke-width:2px,color:#000
    style OC_PW fill:#e1f5ff,stroke:#333,stroke-width:2px,color:#000

    style CH_EXT fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000
    style CH_RELAY fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000
    style CH_WS fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000
    style CH_PW fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000
```

| **属性** | **openclaw 模式** | **chrome 模式** |
|:---|:---|:---|
| **驱动** | `driver: "openclaw"` | `driver: "extension"` |
| **浏览器实例** | OpenClaw 启动新实例 | 使用用户现有 Chrome |
| **数据目录** | `~/.openclaw/browser/openclaw/user-data` | 用户 Chrome 原有 Profile |
| **Cookies/登录** | 空白环境（隔离） | 用户已有的 Cookie 和登录态 |
| **扩展插件** | 无 | 用户已安装的扩展 |
| **启动参数** | `--remote-debugging-port --no-first-run` | 无需额外参数 |
| **生命周期** | OpenClaw 管理启停 | 用户自行管理 Chrome |
| **CDP 端口** | 18800-18899 范围分配 | Relay Server 端口（默认 18792） |
| **使用场景** | 干净的自动化环境 | 需要用户登录态/Cookie |

### 6.3 Chrome 扩展中继架构

```mermaid
sequenceDiagram
    participant AGENT as **Agent**
    participant PW as **Playwright**
    participant RELAY as **Relay Server<br/>localhost:18792**
    participant EXT as **Chrome 扩展**
    participant TAB as **Chrome 标签页**

    Note over EXT,TAB: **用户在 Chrome 中安装 OpenClaw 扩展**

    EXT->>RELAY: **1. WebSocket 连接<br/>ws://localhost:18792/extension<br/>Header: x-openclaw-relay-token**
    RELAY-->>EXT: **2. 连接建立**

    AGENT->>PW: **3. browser action=snapshot**
    PW->>RELAY: **4. CDP 命令<br/>ws://localhost:18792/cdp**
    RELAY->>EXT: **5. 转发 CDP 命令**
    EXT->>TAB: **6. Chrome Extension API<br/>注入到标签页**
    TAB-->>EXT: **7. 返回页面数据**
    EXT-->>RELAY: **8. 返回 CDP 响应**
    RELAY-->>PW: **9. 转发响应**
    PW-->>AGENT: **10. 返回页面快照**

    rect rgb(255, 250, 205)
    Note over RELAY,EXT: **用户需点击扩展图标启用标签页控制<br/>Badge 显示 ON 表示已激活**
    end
```

### 6.4 浏览器启动参数

`openclaw` 模式启动浏览器时使用的关键参数：

```text
--remote-debugging-port={cdpPort}     # CDP 调试端口
--user-data-dir={userDataDir}         # 独立用户数据目录
--no-first-run                        # 跳过首次运行向导
--no-default-browser-check            # 跳过默认浏览器检查
--disable-sync                        # 禁用同步
--headless=new                        # 无头模式（可选）
--no-sandbox                          # 禁用沙箱（可选，Docker 中常用）
```

### 6.5 浏览器工具架构

```mermaid
graph TB
    subgraph "**browser 工具入口**"
        BTOOL[**browser-tool.ts<br/>Agent 工具接口**]
    end

    subgraph "**浏览器目标 Targets**"
        SANDBOX[**sandbox<br/>隔离浏览器实例**]
        HOST[**host<br/>本机浏览器**]
        NODE[**node<br/>远程浏览器代理**]
    end

    subgraph "**浏览器 Profiles**"
        CHROME[**chrome<br/>Chrome 扩展中继**]
        OPENCLAW_P[**openclaw<br/>隔离浏览器 Profile**]
    end

    subgraph "**浏览器操作 Actions**"
        A_STATUS[**status - 状态查询**]
        A_START[**start - 启动浏览器**]
        A_STOP[**stop - 停止浏览器**]
        A_TABS[**tabs - 标签页列表**]
        A_OPEN[**open - 打开 URL**]
        A_NAV[**navigate - 导航**]
        A_SNAP[**snapshot - 页面快照<br/>AI 可读格式**]
        A_SHOT[**screenshot - 截图**]
        A_ACT[**act - 执行操作<br/>点击/输入/滚动**]
        A_PDF[**pdf - 导出 PDF**]
    end

    subgraph "**底层实现**"
        PW[**Playwright<br/>浏览器自动化**]
        CDP[**Chrome DevTools Protocol<br/>CDP 连接**]
        EXT[**Chrome Extension<br/>扩展中继**]
    end

    BTOOL --> SANDBOX
    BTOOL --> HOST
    BTOOL --> NODE

    SANDBOX --> CHROME
    SANDBOX --> OPENCLAW_P

    BTOOL --> A_STATUS
    BTOOL --> A_START
    BTOOL --> A_STOP
    BTOOL --> A_TABS
    BTOOL --> A_OPEN
    BTOOL --> A_NAV
    BTOOL --> A_SNAP
    BTOOL --> A_SHOT
    BTOOL --> A_ACT
    BTOOL --> A_PDF

    A_SNAP --> PW
    A_SHOT --> PW
    A_ACT --> PW
    A_OPEN --> CDP
    A_NAV --> CDP
    HOST --> EXT

    style BTOOL fill:#e1f5ff,stroke:#333,stroke-width:2px,color:#000

    style SANDBOX fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000
    style HOST fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000
    style NODE fill:#ffe1e1,stroke:#333,stroke-width:2px,color:#000

    style CHROME fill:#fff3e1,stroke:#333,stroke-width:2px,color:#000
    style OPENCLAW_P fill:#fff3e1,stroke:#333,stroke-width:2px,color:#000

    style A_STATUS fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style A_START fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style A_STOP fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style A_TABS fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style A_OPEN fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style A_NAV fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style A_SNAP fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style A_SHOT fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style A_ACT fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000
    style A_PDF fill:#e1ffe1,stroke:#333,stroke-width:2px,color:#000

    style PW fill:#f5e1ff,stroke:#333,stroke-width:2px,color:#000
    style CDP fill:#f5e1ff,stroke:#333,stroke-width:2px,color:#000
    style EXT fill:#f5e1ff,stroke:#333,stroke-width:2px,color:#000
```

### 6.6 典型使用时序 - 搜索并浏览

```mermaid
sequenceDiagram
    participant USER as **用户**
    participant AGENT as **Agent**
    participant WSEARCH as **web_search**
    participant WFETCH as **web_fetch**
    participant BROW as **browser**

    USER->>AGENT: **"帮我查找 OpenClaw 的最新文档"**

    rect rgb(240, 248, 255)
    Note over AGENT,WSEARCH: **Step 1: 搜索**
    AGENT->>WSEARCH: **web_search<br/>query="OpenClaw documentation 2026"**
    WSEARCH-->>AGENT: **返回 5 条搜索结果<br/>含标题、URL、摘要**
    end

    rect rgb(240, 255, 240)
    Note over AGENT,WFETCH: **Step 2: 抓取重点页面**
    AGENT->>WFETCH: **web_fetch<br/>url="https://docs.openclaw.com/start"**
    WFETCH-->>AGENT: **返回 Markdown 正文内容**
    end

    alt **需要交互操作**
        rect rgb(255, 250, 240)
        Note over AGENT,BROW: **Step 3: 浏览器操作（可选）**
        AGENT->>BROW: **browser action=open<br/>url="https://docs.openclaw.com"**
        BROW-->>AGENT: **页面已打开**
        AGENT->>BROW: **browser action=snapshot**
        BROW-->>AGENT: **返回 AI 可读快照**
        AGENT->>BROW: **browser action=act<br/>click "Getting Started"**
        BROW-->>AGENT: **操作完成**
        end
    end

    AGENT-->>USER: **"根据搜索和浏览结果，以下是..."**
```

## 7. 配置说明

### 7.1 Web Search 配置

```text
# ~/.openclaw/openclaw.json
{
  "tools": {
    "web": {
      "search": {
        "enabled": true,                    # 启用/禁用
        "provider": "brave",                # "brave" 或 "perplexity"
        "apiKey": "BSA_xxx",                # Brave API Key
        "timeoutSeconds": 30,               # 超时时间
        "cacheTtlMinutes": 15,              # 缓存 TTL
        "maxResults": 5,                    # 最大结果数
        "perplexity": {
          "apiKey": "pplx-xxx",             # Perplexity API Key
          "model": "perplexity/sonar-pro"   # 模型选择
        }
      }
    }
  }
}
```

### 7.2 环境变量

| **变量** | **说明** |
|:---|:---|
| `BRAVE_API_KEY` | Brave Search API 密钥 |
| `PERPLEXITY_API_KEY` | Perplexity API 密钥 |
| `OPENROUTER_API_KEY` | OpenRouter API 密钥（自动路由 Perplexity） |
| `FIRECRAWL_API_KEY` | Firecrawl API 密钥（高级网页抓取） |

## 8. 关键文件索引

| **文件** | **职责** |
|:---|:---|
| `src/agents/tools/web-search.ts` | Web 搜索工具核心实现 |
| `src/agents/tools/web-fetch.ts` | 网页内容抓取工具 |
| `src/agents/tools/web-fetch-utils.ts` | HTML 提取工具（Readability + Markdown） |
| `src/agents/tools/web-shared.ts` | 共享工具（缓存、超时） |
| `src/agents/tools/web-tools.ts` | Web 工具统一导出 |
| `src/agents/tools/browser-tool.ts` | 浏览器自动化工具 |
| `src/browser/` | 浏览器自动化基础设施 |
| `src/browser/chrome.ts` | Chrome 浏览器启动与管理 |
| `src/browser/pw-session.ts` | Playwright CDP 连接管理 |
| `src/browser/extension-relay.ts` | Chrome 扩展中继服务器 |
| `src/browser/profiles.ts` | 浏览器 Profile 管理 |
| `src/browser/detect.ts` | 浏览器可执行文件检测 |
| `src/infra/net/fetch-guard.ts` | SSRF 防护 |
| `src/agents/openclaw-tools.ts` | 工具注册入口 |
